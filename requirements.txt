# 핵심 의존성 — 본인 GPU 머신에서 설치
# CUDA 버전에 맞는 torch는 https://pytorch.org 에서 별도 확인 후 설치 권장
torch>=2.1
torchvision>=0.16
timm>=1.0.0
# DINOv3는 stable transformers에 아직 없을 수 있음.
# 그 경우: pip install git+https://github.com/huggingface/transformers
transformers>=4.44
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65
